#include "stdafx.h"
#include "iostream"
using namespace std;

int main()
{
	setlocale(LC_ALL, "ru");
	double n,a,x=0;
	cout << "Введите n: ";
	cin >> n;
	cout << "Введите a: ";
	cin >> a;
	for (int i=0; i <= n ;i++) {
		x += pow(a,i);
	}
	cout << x << endl;
}
